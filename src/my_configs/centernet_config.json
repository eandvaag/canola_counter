{
    "num_classes": 1,
    "backbone_name": "resnet_50",
    "score_thresh": 0.5,
    "max_detections": 50,

    "learning_rate": 0.0001,


    "num_epochs": 10,
    "early_stopping": false,
    "train_batch_size": 8,
    "inference_batch_size": 8,

    "load_method": "best_val",

    "data_augmentations": [
        {
            "type": "flip_vertical",
            "parameters": {
                "probability": 0.5
            }
        },
        {
            "type": "flip_horizontal",
            "parameters": {
                "probability": 0.5
            }
        }
    ]




}